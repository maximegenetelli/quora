<div class="sidebar d-flex flex-column mr-50">

    <a href="{{ path('app_question_form') }}" class="btn btn-primary btn-icon-primary">
        <i class="fa-solid fa-question mr-5"></i> 
        <span>Poser une question</span>
    </a>

    <ul class="sidebar-nav py-20">

        <a href="{{ path('app_home')}}" 
            {% if app.request.pathinfo == path('app_home') %}
                class="active"
            {% endif %}>
            <i class="fa-solid fa-house mr-5"></i>
            Liste des questions
        </a>
        <a href="#"><i class="fa-solid fa-circle-question mr-5"></i>Mes questions</a>
        <a href="#"><i class="fa-solid fa-puzzle-piece mr-5"></i>Mes réponses</a>
    </ul>

</div>


{# class="{{ app.request.pathinfo == path('app_home') ? 'active' : '' }}" #}